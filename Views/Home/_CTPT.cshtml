@model Home
<h1>Model</h1>
@{
    if(Model!=null)
    {
        <h3 class="p-3 mb-2 bg-success text-white">
        Chi tiết phiểu thuê: @Model.MPT, @Model.MAPhong, @Model.NgayDen, @Model.GioDen, @Model.NgayDi, @Model.MAHD;
</h3>
    }
}